fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20
# Ships the IANA time-zone database so PROBEDECK_TZ works on slim base images
# (python:*-slim carries no /usr/share/zoneinfo).
tzdata==2024.2
